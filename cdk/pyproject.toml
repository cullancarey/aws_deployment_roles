[project]
name = "aws-deployment-roles-cdk"
version = "0.1.0"
description = "CDK app for AWS deployment roles and organization guardrails"
requires-python = ">=3.13,<3.14"
dependencies = [
    "aws-cdk-lib==2.110.1",
    "constructs>=10.0.0,<11.0.0",
]

[tool.uv]
package = false